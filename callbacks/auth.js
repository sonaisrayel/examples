// function authenticateUser(username, password) {
//     if (username === 'user' && password === 'password') {
//       return true;
//     }
//     return false;
//   }
  
//   const isAuthenticated = authenticateUser('user', 'password');
//   console.log(isAuthenticated);

  
  // function authenticateUserAsync(username, password, callback) {
  //   setTimeout(() => {
  //     if (username === 'user' && password === 'password') {
  //       callback(true);
  //     } else {
  //       callback(false);
  //     }
  //   }, 1000); 
  // }
  
  // authenticateUserAsync('user', 'password', (isAuthenticated) => {
  //   console.log(isAuthenticated); 
  // });
  
  
  